<div class="container">
    <div class="jumbotron">
        <h1 class="display-4">Burgers!</h1>
        <hr class="my-4">
        <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
    </div>
    <br>
    <div class="row">
        <div class="col">
            <h2>Hot and Ready:</h2>
            <ul>
                {{#each burgers}}
                    {{#unless devoured}}
                        <li>
                            {{burger_name}}
                            <button class="eat-it" data-id="{{id}}">Devoured</button>
                        </li>
                    {{/unless}}
                {{/each}}
            </ul>
        </div>

        <div class="col">
            <h2>Devoured:</h2>
            <ul>
                {{#each burgers}}
                    {{#if devoured}}
                    <li>
                        {{burger_name}}
                        <h4>It's gone...</h4>
                    </li>
                    {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>   
</div>